{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "lldb-dap",
			"request": "launch",
			"name": "Launch",
			"program": "${workspaceFolder}/bin/debug/main",
			"args": [],
			"preLaunchTask": "build odin main (debug)",
			"cwd": "${workspaceFolder}"
		}
	]
}
