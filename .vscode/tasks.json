{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build odin main (debug)",
      "type": "shell",
      "command": " mkdir -p bin && odin build . -out:bin/main -debug",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
